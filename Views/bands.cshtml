@{
  Layout="Shared/_Layout.cshtml";
}

<h1> Bands </h1>
<hr />
<h4>Bands:</h4>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var band in Model)
    {
      <li><a href="/bands/@band.GetId()">@band.GetName()</a></li>
    }
  </ul>
}

<h4><a href = "/bands/add"> Add new band</a></h4>
